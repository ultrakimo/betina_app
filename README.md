# BETina App
Personal AI companion for GeniusBet players.
Built with Expo (React Native) + Supabase + Claude API.

## Setup
1. Copy `.env.example` to `.env` and fill in credentials
2. `npm install`
3. `npx expo start`
